// Project.allProjects = [];
//
// Project.prototyope.toHtml = function(templateId) {
//   var template = Handlebars.compile($(templateId).text());
//   return template(this);
// };

//complete a constructor function that creates objects from all of my data file.



// PORTFOLIO ASSIGNMENT Six:
// on load check to see if there is anything stored in local data, if so parse that json information and run it back through the constructor, which will push it the DOM
